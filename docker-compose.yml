version: "3.8"
services:
  mongo:
    image: mongo:7
    ports: ["27017:27017"]
    volumes: ["mongo_data:/data/db"]
  server:
    build: ./server
    ports: ["4000:4000"]
    env_file: .env
    depends_on: [mongo]
    volumes: ["./server:/app/server"]
volumes:
  mongo_data:
